console.log("Hello via Bun!");

// connect discord bot
// fetch transactions for each user
// send messages with transaction details
// request new access token if expired
// prompt user to get new code if truelayer access expired
// api wrapper file
